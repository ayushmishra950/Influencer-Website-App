# Optional. Leave unset for device/emulator development — the app derives the
# API host from the Expo dev server automatically.
# Set it for web builds and for any real (production) build.
EXPO_PUBLIC_API_URL=http://localhost:5050
